# opl-whatsapp-server

 To disable testing mode set the environment variable `TESTING` to `false` aka:

Bash:

```bash
export TESTING=false
```

Dockerfile:

```dockerfile
ENV TESTING=false
```

Powershell:

```powershell
$env:TESTING = "false"
```

CMD:

```cmd
set TESTING=false
```
