# opl-whatsapp-server

 To disable testing mode set the environment variable `TESTING` to `off` aka:

Bash:

```bash
export TESTING=off
```

Dockerfile:

```dockerfile
ENV TESTING=off
```

Powershell:

```powershell
$env:TESTING = "off"
```

CMD:

```cmd
set TESTING=off
```
